instalar chocolatey, nodejs, yarn 
$ yarn init -y 
$ yarn add express 
$ yarn add nodemon // atualizar server 
$ yarn add mongoose // banco de dados 
$ yarn add cors //ver servidor
$ yarn add axios
$ yarn dev // rodar servidor
